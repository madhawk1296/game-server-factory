# cp terraform.tfvars.example terraform.tfvars, then fill in.
# terraform.tfvars is gitignored.

do_token = "dop_v1_..."

# region  = "nyc3"          # nyc1/nyc3 east, sfo3 west -- both measured ~60ms
# droplet_size = "s-4vcpu-8gb"   # $48/mo, 4 vCPU / 8 GB
# ssh_allowed_ips = ["203.0.113.4/32"]   # narrow once it works
